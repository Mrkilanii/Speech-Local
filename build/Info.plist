<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>SpeechLocal</string>
    <key>CFBundleDisplayName</key>
    <string>SpeechLocal</string>
    <key>CFBundleExecutable</key>
    <string>SpeechLocal</string>

    <!-- Stable across rebuilds. TCC keys grants to signature + bundle ID; a
         changing identifier makes macOS treat every build as a new app and
         silently drop Accessibility. -->
    <key>CFBundleIdentifier</key>
    <string>dev.kilanii.speechlocal</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSMinimumSystemVersion</key>
    <string>26.0</string>

    <!-- Menu-bar only: no Dock icon, no main window. -->
    <key>LSUIElement</key>
    <true/>

    <key>NSMicrophoneUsageDescription</key>
    <string>SpeechLocal transcribes your speech on-device. Audio never leaves this Mac.</string>

    <key>NSAudioCaptureUsageDescription</key>
    <string>SpeechLocal records meeting audio on-device so it can transcribe and summarise it. Audio never leaves this Mac.</string>

    <key>NSHighResolutionCapable</key>
    <true/>
</dict>
</plist>
